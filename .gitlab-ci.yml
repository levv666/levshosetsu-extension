# ... (rest of your file remains the same) ...

image: eclipse-temurin:21-noble

stages:
  - validate
  - test
  - deploy

before_script:
  - apt update; apt install -y git
  # (Keeping your original logic for FILES)
  - if [ -z "$CI_MERGE_REQUEST_TARGET_BRANCH_NAME" ]; then
      export FILES=($(git diff-tree --no-commit-id --name-only -r $CI_COMMIT_SHA | grep '^src/.*\.lua$'));
    else
      git fetch origin $CI_MERGE_REQUEST_TARGET_BRANCH_NAME;
      export FILES=($(git diff --name-only $CI_MERGE_REQUEST_TARGET_BRANCH_NAME...$CI_COMMIT_SHA | grep '^src/.*\.lua$'));
    fi

download:
  stage: .pre
  script:
    - ./dev-setup.sh --tester
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: push
    paths:
      - bin
  rules:
    - when: always

# --- MODIFIED JOB STARTS HERE ---
index:
  stage: validate
  script:
    # 1. Generate the index as usual
    - java -jar bin/extension-tester.jar --ci --generate-index
    
    # 2. Check if there are changes
    - DELTA=$(git diff --minimal -- index.json)
    
    # 3. If changes exist, commit and push them
    - |
      if [ ! -z "$DELTA" ]; then
        echo "Index file is out of date. Auto-fixing..."
        
        # Configure a dummy git user so we can commit
        git config user.email "ci-bot@example.com"
        git config user.name "CI Bot"
        
        # Stage the file
        git add index.json
        
        # Commit with [skip ci] to prevent infinite loops
        git commit -m "chore: auto-update index.json [skip ci]"
        
        # Push the change back to the current branch using the Job Token
        git push "https://gitlab-ci-token:${CI_JOB_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git" HEAD:${CI_COMMIT_REF_NAME}
        
        echo "Successfully pushed updated index.json."
      else
        echo "Index is up to date."
      fi
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: pull
    paths:
      - bin
  rules:
    - when: always
# --- MODIFIED JOB ENDS HERE ---

metadata:
  stage: validate
# ... (rest of your file remains the same) ...